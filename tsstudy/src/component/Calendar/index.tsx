import React, { Fragment } from "react";
import moment from "moment";
import { DateInfo } from "types/DateInfo.types";

const Calendar = () => {
  //const {} = props;

  return <div></div>;
};

export default Calendar;
